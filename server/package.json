{
  "name": "logmate-api-server",
  "version": "1.0.0",
  "private": true,
  "description": "ORS proxy + PayFast subscription billing (checkout/webhook) for LogMate",
  "main": "api-server.js",
  "scripts": {
    "start": "node api-server.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-fetch": "^2.7.0"
  }
}
